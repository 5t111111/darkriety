machine:
  services:
    - docker

dependencies:
  override:
    - docker build -t 5t111111/darkriety:$CIRCLE_SHA1 .

test:
  override:
    - echo "no test :P"

deployment:
  hub:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push 5t111111/darkriety:$CIRCLE_SHA1
