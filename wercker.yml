box: wercker-labs/docker
build:
  steps:
    - script:
        name: docker version
        code: |
          sudo docker version
    - script:
        name: docker build
        code: |
          sudo docker build -t 5t111111/darkriety .
    - script:
        name: list images
        code: |
          sudo docker images
